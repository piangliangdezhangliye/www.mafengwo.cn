laydate.render({
    elem: '#hotel-center',
    // type: 'datetime',
    value: '未定',
    trigger: 'click',//采用click弹出
    // range: true
    theme: '#ff9d00',
    calendar: true,
    showBottom: false,
    type:'date',
    range: true
  });